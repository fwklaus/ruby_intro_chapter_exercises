# In the code below, two arrays containing several numbers are assigned to
# two variables, array1 and array2.

array1 = [1, 5, 9]
array2 = [1, 9, 5]


array1 == array2 ? puts(true) : puts(false)

#------------------------------------------

puts array1 == array2
